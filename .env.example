# .env.example — Project DRISHTI
# Copy to .env and fill in real values before running.
# Never commit the real .env to version control.

# ── PostgreSQL (Day 2) ────────────────────────
DATABASE_URL=postgresql+asyncpg://drishti:drishti123@localhost:5432/drishti_db

# ── API Config ────────────────────────────────
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=true

# ── ML Model Paths (Day 2-3) ─────────────────
NLP_MODEL_PATH=models/hingbert
XGBOOST_MODEL_PATH=models/time_predictor.json
GRAPH_DATA_PATH=data/complaints.csv
